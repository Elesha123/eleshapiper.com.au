# Required for the Weekly LinkedIn Engine's serverless function
# (netlify/functions/generate-linkedin-posts.mts).
#
# Set the real value in Netlify's dashboard under
# Site configuration > Environment variables — never commit the actual key.
ANTHROPIC_API_KEY=
